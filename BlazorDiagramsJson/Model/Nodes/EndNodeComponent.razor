<div class="custom-node @(Node.Selected ? " selected" : "")">
    <div class="card" style="height: 100px; width: 250px; border-color: red; border-radius: 50%;">
        <div class="card-body">
            <h5 class="card-title" style="text-align: center;">End Node</h5>
            <h5 class="card-title" style="text-align: center;">
                @if (!String.IsNullOrEmpty(Node.Title)) { 
                    <span>Title: @Node.Title</span> 
                }
            </h5>
        </div>

    </div>

    @foreach (var port in Node.Ports)
    {
        <PortRenderer Port="port"></PortRenderer>
    }
</div>

@code {
    [Parameter]
    public EndNode Node { get; set; }
}
